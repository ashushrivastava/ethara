# Railway dashboard: connect this repo, set Root Directory to `server`,
# then add variables DATABASE_URL and JWT_SECRET (see README).
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
